// array.h
#ifndef ARRAY_H
#define ARRAY_H

#include <vector>
#include <unordered_map>

class ArraySolutions {
public:
    std::vector<int> twoSum(const std::vector<int>& nums, int target);
};

#endif // ARRAY_H
